{
  "name": "training-sls-martin-perez-back",
  "version": "1.0.0",
  "description": "alegra serverless training",
  "main": "index.js",
  "scripts": {
    "off": "serverless offline",
    "deploy": "npm run build:layer && serverless deploy",
    "build:layer": "rm -rf dist && tsc",
    "dev":"npm run build:layer && npm run off"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.114",
    "esbuild": "^0.17.18",
    "serverless": "^3.30.1",
    "serverless-esbuild": "^1.43.1",
    "serverless-latest-layer-version": "^2.1.2",
    "serverless-offline": "^12.0.4",
    "typescript": "^4.6.2"
  }
}
